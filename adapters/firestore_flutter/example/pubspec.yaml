name: example
description: A new Flutter project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  database_adapter_firestore_flutter:
    path: '../'
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter

flutter: